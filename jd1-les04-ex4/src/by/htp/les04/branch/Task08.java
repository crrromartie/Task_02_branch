package by.htp.les04.branch;

public class Task08 {

	public static void task() {
		double a = 5;
		double b = 6;
		if (Math.pow(a, 2) < Math.pow(b, 2)) {
			System.out.println("Least 2 A = " + Math.pow(a, 2));
		} else if (Math.pow(a, 2) < Math.pow(a, 2)) {
			System.out.println("Least 2 B = " + Math.pow(b, 2));
		} else {
			System.out.println("Equal");
		}

	}

}
